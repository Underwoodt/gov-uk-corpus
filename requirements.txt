# Postgres driver: the code uses psycopg 3 (import psycopg / psycopg.rows.dict_row),
# not psycopg2. Range (not an exact pin) so it matches the 3.x already on the box.
psycopg[binary]>=3.1,<4
requests==2.31.0
python-dotenv==1.0.0
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
APScheduler==3.10.4
html2text==2024.2.26
sqlparse==0.5.5
jinja2==3.1.4
python-multipart==0.0.9
argon2-cffi>=23,<25          # Argon2id password hashing (user accounts / auth)
anthropic[bedrock]>=0.40,<1.0
openpyxl==3.1.5
